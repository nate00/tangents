<%= div_for post do %>
  <%= raw(post.body_with_links) %>
  <!--span class='actions'>
    <%#= link_to "Edit", edit_post_path(post) %>
    <%#= link_to "Delete", post, confirm: "Are you sure?", method: :delete %>
  </span-->

  <!-- I don't think this should go here. -->
  <div id="footnote_ids_hidden" class="hidden">
    <%= post.footnote_ids.to_json() %>
  </div>
<% end %>
