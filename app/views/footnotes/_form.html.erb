<%= form_for(@footnote) do |f| %>
  <% if @footnote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@footnote.errors.count, "error") %> prohibited this footnote from being saved:</h2>

      <ul>
      <% @footnote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :index %><br />
    <%= f.number_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
